.idea/
dem/
prompts/

# Environment files (see dotenv). The *.local files may hold secrets, so
# they are explicity ignored; shared .env and .env.<env> files are committed.
.env.local
.env.*.local

# build products
/hexover
/mapgen
/tdb
/tpty
/dist/

# generated from api/openapi.yaml via `make generate`; wired in by #76, not #71
/internal/api/openapi.gen.go

# Hugo
/public/
/resources/_gen/
/.hugo_build.lock
hugo_stats.json
